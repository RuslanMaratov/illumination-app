export const findEta1 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.26;

    case index >= 0.56 && index <= 0.6:
      return 0.3;

    case index >= 0.61 && index <= 0.7:
      return 0.34;

    case index >= 0.71 && index <= 0.8:
      return 0.38;

    case index >= 0.81 && index <= 0.9:
      return 0.4;

    case index >= 0.91 && index <= 1:
      return 0.43;

    case index >= 1.01 && index <= 1.1:
      return 0.46;

    case index >= 1.11 && index <= 1.25:
      return 0.5;

    case index >= 1.26 && index <= 1.5:
      return 0.54;

    case index >= 1.55 && index <= 1.75:
      return 0.57;

    case index >= 1.76 && index <= 2:
      return 0.6;

    case index >= 2.01 && index <= 2.25:
      return 0.62;

    case index >= 2.26 && index <= 2.5:
      return 0.64;

    case index >= 2.51 && index <= 3:
      return 0.68;

    case index >= 3.01 && index <= 3.5:
      return 0.7;

    case index >= 3.51 && index <= 4:
      return 0.72;

    case index >= 4.01 && index <= 5:
      return 0.75;

    default:
      break;
  }
};

export const findEta2 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.25;

    case index >= 0.56 && index <= 0.6:
      return 0.28;

    case index >= 0.61 && index <= 0.7:
      return 0.32;

    case index >= 0.71 && index <= 0.8:
      return 0.36;

    case index >= 0.81 && index <= 0.9:
      return 0.38;

    case index >= 0.91 && index <= 1:
      return 0.41;

    case index >= 1.01 && index <= 1.1:
      return 0.43;

    case index >= 1.11 && index <= 1.25:
      return 0.46;

    case index >= 1.26 && index <= 1.5:
      return 0.49;

    case index >= 1.55 && index <= 1.75:
      return 0.52;

    case index >= 1.76 && index <= 2:
      return 0.54;

    case index >= 2.01 && index <= 2.25:
      return 0.56;

    case index >= 2.26 && index <= 2.5:
      return 0.58;

    case index >= 2.51 && index <= 3:
      return 0.6;

    case index >= 3.01 && index <= 3.5:
      return 0.62;

    case index >= 3.51 && index <= 4:
      return 0.64;

    case index >= 4.01 && index <= 5:
      return 0.66;

    default:
      break;
  }
};

export const findEta3 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.2;

    case index >= 0.56 && index <= 0.6:
      return 0.24;

    case index >= 0.61 && index <= 0.7:
      return 0.28;

    case index >= 0.71 && index <= 0.8:
      return 0.31;

    case index >= 0.81 && index <= 0.9:
      return 0.34;

    case index >= 0.91 && index <= 1:
      return 0.37;

    case index >= 1.01 && index <= 1.1:
      return 0.39;

    case index >= 1.11 && index <= 1.25:
      return 0.42;

    case index >= 1.26 && index <= 1.5:
      return 0.47;

    case index >= 1.55 && index <= 1.75:
      return 0.51;

    case index >= 1.76 && index <= 2:
      return 0.54;

    case index >= 2.01 && index <= 2.25:
      return 0.57;

    case index >= 2.26 && index <= 2.5:
      return 0.59;

    case index >= 2.51 && index <= 3:
      return 0.63;

    case index >= 3.01 && index <= 3.5:
      return 0.66;

    case index >= 3.51 && index <= 4:
      return 0.68;

    case index >= 4.01 && index <= 5:
      return 0.72;

    default:
      break;
  }
};

export const findEta4 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.19;

    case index >= 0.56 && index <= 0.6:
      return 0.23;

    case index >= 0.61 && index <= 0.7:
      return 0.27;

    case index >= 0.71 && index <= 0.8:
      return 0.3;

    case index >= 0.81 && index <= 0.9:
      return 0.33;

    case index >= 0.91 && index <= 1:
      return 0.35;

    case index >= 1.01 && index <= 1.1:
      return 0.37;

    case index >= 1.11 && index <= 1.25:
      return 0.4;

    case index >= 1.26 && index <= 1.5:
      return 0.44;

    case index >= 1.55 && index <= 1.75:
      return 0.47;

    case index >= 1.76 && index <= 2:
      return 0.5;

    case index >= 2.01 && index <= 2.25:
      return 0.52;

    case index >= 2.26 && index <= 2.5:
      return 0.54;

    case index >= 2.51 && index <= 3:
      return 0.57;

    case index >= 3.01 && index <= 3.5:
      return 0.59;

    case index >= 3.51 && index <= 4:
      return 0.61;

    case index >= 4.01 && index <= 5:
      return 0.64;

    default:
      break;
  }
};

export const findEta5 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.17;

    case index >= 0.56 && index <= 0.6:
      return 0.2;

    case index >= 0.61 && index <= 0.7:
      return 0.22;

    case index >= 0.71 && index <= 0.8:
      return 0.24;

    case index >= 0.81 && index <= 0.9:
      return 0.26;

    case index >= 0.91 && index <= 1:
      return 0.28;

    case index >= 1.01 && index <= 1.1:
      return 0.3;

    case index >= 1.11 && index <= 1.25:
      return 0.32;

    case index >= 1.26 && index <= 1.5:
      return 0.34;

    case index >= 1.55 && index <= 1.75:
      return 0.36;

    case index >= 1.76 && index <= 2:
      return 0.38;

    case index >= 2.01 && index <= 2.25:
      return 0.39;

    case index >= 2.26 && index <= 2.5:
      return 0.4;

    case index >= 2.51 && index <= 3:
      return 0.42;

    case index >= 3.01 && index <= 3.5:
      return 0.43;

    case index >= 3.51 && index <= 4:
      return 0.45;

    case index >= 4.01 && index <= 5:
      return 0.46;

    default:
      break;
  }
};

export const findEta6 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.13;

    case index >= 0.56 && index <= 0.6:
      return 0.16;

    case index >= 0.61 && index <= 0.7:
      return 0.19;

    case index >= 0.71 && index <= 0.8:
      return 0.21;

    case index >= 0.81 && index <= 0.9:
      return 0.23;

    case index >= 0.91 && index <= 1:
      return 0.25;

    case index >= 1.01 && index <= 1.1:
      return 0.26;

    case index >= 1.11 && index <= 1.25:
      return 0.28;

    case index >= 1.26 && index <= 1.5:
      return 0.31;

    case index >= 1.55 && index <= 1.75:
      return 0.33;

    case index >= 1.76 && index <= 2:
      return 0.35;

    case index >= 2.01 && index <= 2.25:
      return 0.37;

    case index >= 2.26 && index <= 2.5:
      return 0.38;

    case index >= 2.51 && index <= 3:
      return 0.4;

    case index >= 3.01 && index <= 3.5:
      return 0.41;

    case index >= 3.51 && index <= 4:
      return 0.42;

    case index >= 4.01 && index <= 5:
      return 0.44;

    default:
      break;
  }
};

export const findEta7 = (index) => {
  switch (true) {
    case index >= 0.5 && index <= 0.55:
      return 0.06;

    case index >= 0.56 && index <= 0.6:
      return 0.08;

    case index >= 0.61 && index <= 0.7:
      return 0.1;

    case index >= 0.71 && index <= 0.8:
      return 0.11;

    case index >= 0.81 && index <= 0.9:
      return 0.12;

    case index >= 0.91 && index <= 1:
      return 0.13;

    case index >= 1.01 && index <= 1.1:
      return 0.14;

    case index >= 1.11 && index <= 1.25:
      return 0.15;

    case index >= 1.26 && index <= 1.5:
      return 0.17;

    case index >= 1.55 && index <= 1.75:
      return 0.18;

    case index >= 1.76 && index <= 2:
      return 0.19;

    case index >= 2.01 && index <= 2.25:
      return 0.2;

    case index >= 2.26 && index <= 2.5:
      return 0.21;

    case index >= 2.51 && index <= 3:
      return 0.22;

    case index >= 3.01 && index <= 3.5:
      return 0.23;

    case index >= 3.51 && index <= 4:
      return 0.24;

    case index >= 4.01 && index <= 5:
      return 0.25;

    default:
      break;
  }
};
